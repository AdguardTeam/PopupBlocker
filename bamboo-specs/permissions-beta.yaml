---
version: 2
deployment:
  name: popup blocker - deploy beta
deployment-permissions:
  - groups:
      - extensions-developers
    permissions:
      - view
environment-permissions:
  - userscripts.adtidy.org:
      - groups:
          - extensions-developers
        permissions:
          - view
          - deploy
