/**
 * Non-color design tokens.
 *
 * Colors live in `theme.pcss` (semantic tokens) and `palette.pcss` (base scale);
 * CSS mask icons live in `icons.pcss`; inline SVG icons live in `pages/common/components/Icons/Icons.tsx`.
 */
:root {
    --t3: 0.3s ease;
}
