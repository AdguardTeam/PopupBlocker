/**
 * @fileoverview
 */

/**
 * @param KEY It is used for communication between a script in a page's context and a script in its child frame's context
 * @param bridge_key It is used for communication between a script in userscript host's sandboxed context and the page's context.
 */
export default function popupBlocker(window, KEY, _BRIDGE_KEY) {
    "CONTENT";
}
